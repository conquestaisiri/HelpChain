I want you to update the HelpChain fee-calculation system so that the PTF (Protected Transaction Fee) is calculated correctly for any budget amount entered by the user, no matter how high it is. Right now, the calculation stops working after ₦25,000, so I want you to replace all old hard-coded fee logic with the new universal fee calculator below.

function calculatePTF(budget) {
  if (typeof budget !== 'number' || isNaN(budget) || budget < 5) {
    throw new Error('Invalid budget. Budget must be a number >= 5');
  }

  const b = Math.floor(budget);

  if (b <= 999) return 15;
  if (b <= 4999) return 60;
  if (b <= 9999) return 150;
  if (b <= 14999) return 200;
  if (b <= 19999) return 250;
  if (b <= 24999) return 300;

  // Fallback rule for ANY AMOUNT above ₦25,000
  const baseFee = 300;       // fee for budgets up to ₦25,000  
  const addPer5k = 60;       // additional ₦60 for every 5k or fraction above 25k  
  const excess = b - 25000;
  const chunks = Math.ceil(excess / 5000);
  return baseFee + (addPer5k * chunks);
}


2. Replace the old calculation code everywhere with this function.

Anywhere the old code is used to compute:

PTF

Total amount to deposit

Review screen values

Admin view values

…delete it and use this new function.

3. The UI must use this function too.

On the "Create Offer" screen, whenever the user types a new budget amount:

Call calculatePTF(budget)

Show:

Budget: ₦X

PTF: ₦Y

Total to Deposit: ₦(X + Y)

Make sure the total updates instantly with every change.

4. On offer creation:

When the user submits an offer:

Call calculatePTF(budget) again.

Charge total = budget + ptf.

Store in the database:

budget_amount

ptf_fee

total_deposit

The platform should keep the PTF as revenue.

The user wallet should show only the budget, not the PTF.

5. Admin Review Logic:

If admin approves the offer:

Publish offer.

If admin rejects the offer:

Refund ONLY the budget amount back to the user wallet.

DO NOT refund the PTF.

Record a refund transaction.

6. Test Cases (please implement these in code or console):
calculatePTF(500)      -> 15
calculatePTF(2000)     -> 60
calculatePTF(7000)     -> 150
calculatePTF(15000)    -> 200
calculatePTF(25000)    -> 300
calculatePTF(30000)    -> 360
calculatePTF(48000)    -> 420
calculatePTF(100000)   -> should return a valid number (no more "undefined")


If any of these return undefined or errors, fix immediately.

7. Summary of requirement:

The fee calculator must NEVER stop working regardless of how large the budget amount is.
Every amount above ₦25,000 must be handled automatically using the fallback tier.